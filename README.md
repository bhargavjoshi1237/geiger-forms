# Geiger Forms

Standalone JavaScript Next.js app for confidential project-bound form creation, submission, and review.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run lint`

The prototype stores forms and responses in browser local storage.
